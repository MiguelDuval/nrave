# Android AUX / Samplers fix

The LateNightQML Android path now renders the Samplers and Mic/AUX racks directly from the [Skin] `show_samplers` and `show_microphones` controls in the application window, avoiding desktop SplitView-only geometry for these mobile panels.
